package com.shopstack.entity;

/**
 * QualityCheckResult — Outcome of warehouse staff quality inspection on returned items.
 */
public enum QualityCheckResult {
    ACCEPTED,
    DAMAGED,
    REJECTED,
    PARTIALLY_ACCEPTED
}
