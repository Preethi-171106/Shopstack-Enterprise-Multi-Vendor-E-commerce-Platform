# ShopStack Frontend — Development Environment
# This file is loaded by Vite automatically when running: npm run dev
# DO NOT commit real secrets here. Safe to commit because it only has local dev URLs.

# Backend API base URL for local development
VITE_API_BASE_URL=http://localhost:8080/api
